Jenkins pipeline 

pipeline{
    agent any
    
    environment{
        DOCKERHUB_USERNAME='AnkumpetaVimala'
        APP_NAME='Task1-Interncareer'
        IMAGE_NAME="${DOCKERHUB_USERNAME}/${APP_NAME}"
}
}
